# AIQuant 快速参考

## 常用命令

### 1. 快速测试
```python
# 测试安装
python test_install.py

# 运行示例
python examples/example_basic.py

# 快速获取数据
python -c "import akshare as ak; print(ak.stock_zh_a_hist(symbol='000001', start_date='20241101', end_date='20241122').tail())"
```

### 2. 获取股票数据
```python
import akshare as ak

# 获取A股实时行情
df = ak.stock_zh_a_spot_em()

# 获取个股历史数据 (前复权)
df = ak.stock_zh_a_hist(
    symbol="000001",           # 股票代码
    period="daily",            # daily/weekly/monthly
    start_date="20240101",     # 开始日期
    end_date="20241122",       # 结束日期
    adjust="qfq"               # qfq(前复权)/hfq(后复权)/""(不复权)
)

# 获取股票基本信息
info = ak.stock_individual_info_em(symbol="000001")

# 获取财务报表
balance = ak.stock_balance_sheet_by_report_em(symbol="000001")  # 资产负债表
profit = ak.stock_profit_sheet_by_report_em(symbol="000001")    # 利润表
cash = ak.stock_cash_flow_sheet_by_report_em(symbol="000001")   # 现金流量表
```

### 3. 使用封装好的模块
```python
from src.data_fetch.stock_data import StockDataFetcher
from src.analysis.technical import TechnicalAnalyzer
from src.visualization.charts import ChartVisualizer

# 创建对象
fetcher = StockDataFetcher()
analyzer = TechnicalAnalyzer()
visualizer = ChartVisualizer()

# 获取数据
df = fetcher.get_stock_hist("000001", start_date="20240101", end_date="20241122")

# 计算技术指标
df = analyzer.calculate_all_indicators(df)

# 可视化
visualizer.plot_candlestick(df.tail(60), title="平安银行", ma_periods=[5, 10, 20])
visualizer.plot_macd(df.tail(60))
visualizer.plot_kdj(df.tail(60))
```

## 常用股票代码

### 银行股
- 000001 - 平安银行
- 601398 - 工商银行
- 601939 - 建设银行
- 601288 - 农业银行

### 白酒股
- 600519 - 贵州茅台
- 000858 - 五粮液
- 000568 - 泸州老窖
- 002304 - 洋河股份

### 科技股
- 000063 - 中兴通讯
- 002415 - 海康威视
- 300750 - 宁德时代
- 688981 - 中芯国际

### 地产股
- 000002 - 万科A
- 600048 - 保利地产
- 001979 - 招商蛇口

## 技术指标说明

### MA (移动平均线)
- **MA5**: 5日均线,短期趋势
- **MA10**: 10日均线,短中期趋势
- **MA20**: 20日均线,中期趋势
- **MA60**: 60日均线,长期趋势

**使用**: 价格在均线上方为多头,下方为空头

### MACD (指数平滑异同移动平均线)
- **DIF**: 快线 (12日EMA - 26日EMA)
- **DEA**: 慢线 (DIF的9日EMA)
- **MACD**: 柱状图 (2*(DIF-DEA))

**使用**: 
- DIF上穿DEA: 金叉,买入信号
- DIF下穿DEA: 死叉,卖出信号
- MACD柱状图由负转正: 多头信号

### RSI (相对强弱指标)
- **范围**: 0-100
- **超买**: RSI > 70
- **超卖**: RSI < 30
- **中性**: 30 < RSI < 70

### KDJ (随机指标)
- **范围**: 0-100
- **超买**: K>80, D>80, J>100
- **超卖**: K<20, D<20, J<0
- **金叉**: K线上穿D线,买入信号
- **死叉**: K线下穿D线,卖出信号

### BOLL (布林带)
- **上轨**: 中轨 + 2倍标准差
- **中轨**: 20日均线
- **下轨**: 中轨 - 2倍标准差

**使用**:
- 价格触及上轨: 超买,可能回调
- 价格触及下轨: 超卖,可能反弹
- 布林带收窄: 盘整,可能突破

## 数据保存和加载

```python
from src.utils.helpers import save_to_csv, load_from_csv

# 保存数据
save_to_csv(df, "000001_data.csv", "./data/")

# 加载数据
df = load_from_csv("000001_data.csv", "./data/")
```

## 常见问题

### Q1: 获取数据失败?
A: 检查网络连接,或稍后重试。AkShare数据来自公开源,可能有访问限制。

### Q2: 日期格式错误?
A: 使用 YYYYMMDD 格式,如 "20241122"

### Q3: 找不到股票代码?
A: A股代码为6位数字,深市以000、002、300开头,沪市以600、601、688开头

### Q4: 图表中文显示乱码?
A: 已在 `charts.py` 中配置中文字体,如仍有问题,检查系统是否安装了微软雅黑字体

### Q5: 如何获取更多数据?
A: 查看 [AkShare 官方文档](https://akshare.akfamily.xyz/) 了解所有可用接口

## 进阶功能

### 1. 多股票对比
```python
symbols = ['000001', '600519', '000858']
for symbol in symbols:
    df = fetcher.get_stock_hist(symbol)
    # 处理数据...
```

### 2. 批量分析
```python
# 获取所有股票列表
all_stocks = fetcher.get_stock_list()

# 筛选条件
filtered = all_stocks[all_stocks['涨跌幅'] > 5]
```

### 3. 策略回测
```python
# 简单均线交叉策略
df['signal'] = 0
df.loc[df['MA5'] > df['MA20'], 'signal'] = 1  # 买入
df.loc[df['MA5'] < df['MA20'], 'signal'] = -1  # 卖出
```

## 更多资源

- 📖 查看 `README.md` 了解项目概览
- 📝 查看 `SETUP_GUIDE.md` 了解详细配置
- 📓 查看 `notebooks/01_quick_start.ipynb` 交互式教程
- 💻 查看 `examples/example_basic.py` 完整示例
- 🌐 访问 [AkShare 官方文档](https://akshare.akfamily.xyz/)
